"""Cellpose QC slide deck builder.

Render side-by-side composites of (raw channel XY MIP | cellpose mask)
into a PowerPoint deck for visual QC of segmentation coverage.

See ``build_cellpose_qc_deck.py`` for the CLI entrypoint and
``mip_mask_compositor.py`` for the dataset-agnostic image helpers.
"""
